import {} from "@testing-library/react";

describe("sotrage token", () => {
  it("認証トークン格納", () => {
    throw new Error("テストが実装されていない");
  });
  it("認証トークン取得", () => {
    throw new Error("テストが実装されていない");
  });
  it("認証チェック", () => {
    throw new Error("テストが実装されていない");
  });
});
